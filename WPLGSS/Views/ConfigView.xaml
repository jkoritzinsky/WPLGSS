<UserControl x:Class="WPLGSS.Views.ConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPLGSS.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
  <DockPanel>
    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
      <Button Content="Create New Channel" VerticalAlignment="Top" />
    </StackPanel>
    <StackPanel>
      <TextBlock Text="Input Channels" />
      <DataGrid ItemsSource="{Binding InputChannels}" />
      <TextBlock Text="Output Channels" />
      <DataGrid ItemsSource="{Binding OutputChannels}" />
    </StackPanel>
  </DockPanel>
</UserControl>
